# Build from repo root so npm workspaces resolves @smartbudget/shared
[build]
  command = "npm install --legacy-peer-deps && npm run build --workspace=@smartbudget/web"
  publish = "frontend/.next"

[functions]
  directory = "frontend/.netlify/functions-internal"

[[plugins]]
  package = "@netlify/plugin-nextjs"
